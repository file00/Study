// Print Characters (Input & Output Version)

#include <stdio.h>

int main() {
    
    char chr;

    // Taking a single character as input
    printf("Enter a character: ");
    scanf("%c", &chr);

    // Printing the entered character
    printf("character = %c\n", chr);

    return 0;
}